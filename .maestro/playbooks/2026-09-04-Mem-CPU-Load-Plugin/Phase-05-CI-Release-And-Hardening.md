# Phase 05: CI, Release Automation, and Hardening

This phase makes the project shippable and trustworthy. GitHub Actions runs format, lint, tests, and a smoke test of the real binary on Linux, macOS, and Windows, which is the first time the macOS and Windows backends from Phase 03 actually execute. A tag-driven release workflow publishes prebuilt binaries, integration tests exercise the CLI end to end, and a final review pass tightens error handling, documentation, and repository hygiene so `herdr plugin install thewtex/herdr-mem-cpu-load` works for anyone with a Rust toolchain.

## Tasks

- [x] Add end-to-end integration tests in `tests/cli.rs` that run the built binary through `env!("CARGO_BIN_EXE_herdr-mem-cpu-load")` with `std::process::Command` (no extra test dependencies):
  - Default invocation with `--interval 1` exits 0 and stdout matches the one-line regex from Phase 01; `--graph-style blocks` output contains `▕` and `▏`; `-v` output contains exactly one character from ` ▁▂▃▄▅▆▇█▲` between the frame characters; `-m 2` prints a percent; `-a 0` prints no load averages; `-t 1` on a multi-core machine can exceed 100 percent (assert only that it parses as a number).
  - Invalid arguments (`-a 5`, `-m 9`, `--graph-style nope`, `-i 0`) exit non-zero with a message on stderr and nothing on stdout.
  - `--print-config` emits TOML that round-trips through `toml::from_str::<FileConfig>` (add `toml` as a dev-dependency only if it is not already a normal dependency).
  - `--daemon` with `HERDR_BIN_PATH` pointing at a small fake script written by the test into a temp dir (a shell script on Unix, a `.cmd` on Windows) that records its argv to a file and answers `workspace list` with a canned two-workspace JSON: run the daemon with `--interval 1` for 3 seconds, kill it, and assert the recorded argv contains `report-metadata` calls for both workspace ids with `--source system-monitor`, `--ttl-ms`, `--seq`, and the expected `--token` names. Use `cfg(unix)`/`cfg(windows)` helpers to write the fake in the right language.
  - Verify the fake-herdr "server gone" path: make the fake exit 1 and assert the daemon exits 0 within `max_failures * interval + 2` seconds.
  - **Done.** `tests/cli.rs` holds 10 tests over `env!("CARGO_BIN_EXE_herdr-mem-cpu-load")` and pulls in no new dependency: the Phase 01 regex is hand-matched by `matches_one_line_format`, and a `TempDir` guard gives every test its own `HERDR_PLUGIN_CONFIG_DIR`/`HERDR_PLUGIN_STATE_DIR` so a machine-local `config.toml` cannot change the assertions and two daemon tests cannot fight over the singleton lock.
  - The CPU field of the regex also accepts the six-wide integer form `cpu_text` documents for values at or above 100%. A runner pegged for the whole sampling window would otherwise fail on formatting the binary got right.
  - `toml` was already a normal dependency, so nothing was added to `[dev-dependencies]`; `--print-config` output is parsed with `toml::from_str::<FileConfig>` and its keys checked.
  - The fake herdr is written by `write_fake_herdr`, `cfg(unix)` as an `sh` script and `cfg(windows)` as a `.cmd`. Both append one line of argv per invocation and answer `workspace list` with a canned two-workspace envelope. The daemon runs for 3 seconds at `--interval 1`; the assertions find a `report-metadata` call for `w1` and for `w9` carrying `--source system-monitor`, `--ttl-ms`, `--seq`, `--clear-token`, and the `cpu_status`, `mem_status`, `load_status`, `sys_status`, and `cpu_history` token names.
  - The "server gone" test makes the fake exit 1 and polls `try_wait` until the daemon exits 0, inside `DEFAULT_MAX_FAILURES + 2` seconds. It exits after about 5.

- [x] Create `.github/workflows/ci.yml` modeled on the structure of `/home/matt/src/herdr/.github/workflows/ci.yml` but much smaller:
  - Trigger on `push` to `main` and on `pull_request`; `permissions: contents: read`; concurrency group cancelling in-progress runs per ref.
  - Job `check` with a matrix of `ubuntu-latest`, `macos-latest`, and `windows-latest`: checkout (pin actions by SHA like the herdr workflow does), install the stable toolchain via `dtolnay/rust-toolchain@stable` with `clippy` and `rustfmt` components, cache with `Swatinem/rust-cache`, then `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test --locked`, `cargo build --release --locked`, and a smoke step that runs the release binary with `--interval 1` and `--graph-style blocks` and prints the output (use `bash` shell on all three runners so the step is identical).
  - Job `cross-check` on `ubuntu-latest` running `cargo check` for `x86_64-unknown-freebsd` to keep the unsupported stub compiling (allow it to be skipped if the target's rust-std is unavailable, using `continue-on-error: true`).
  - Job `manifest` on `ubuntu-latest` that validates `herdr-plugin.toml` parses as TOML and contains the required keys (`id`, `name`, `version`, `min_herdr_version`) using a short Python script, and that `version` in `herdr-plugin.toml` equals `version` in `Cargo.toml`.
  - **Done.** `.github/workflows/ci.yml` has the three jobs. Every action is pinned to the same commit SHA the herdr workflow pins: `actions/checkout@df4cb1c0` (v6), `dtolnay/rust-toolchain@29eef336` (v1), `Swatinem/rust-cache@e18b4977` (v2).
  - `dtolnay/rust-toolchain` takes its toolchain from the action *ref*, so a SHA-pinned use cannot rely on `@stable` naming it; the step passes `toolchain: stable` explicitly instead, which is the same thing the herdr workflow does with its pinned version.
  - The smoke step is one `shell: bash` block on all three runners, appending `.exe` to the binary path when `$RUNNER_OS` is `Windows`. It runs the release binary once plain and once with `--graph-style blocks` and prints both.
  - `cross-check` installs the `x86_64-unknown-freebsd` target and runs `cargo check --locked --target x86_64-unknown-freebsd` under `continue-on-error: true`. Verified locally: it compiles.
  - `manifest` runs `scripts/check_manifest.py`, which parses `herdr-plugin.toml` with `tomllib`, requires `id`, `name`, `version`, and `min_herdr_version` to be non-empty strings, and fails when the manifest version differs from `[package] version` in `Cargo.toml`, pointing at `scripts/release.sh`. Verified locally.

- [x] Create `.github/workflows/release.yml` triggered by tags matching `v*`:
  - Build release binaries for `x86_64-unknown-linux-gnu`, `aarch64-unknown-linux-gnu` (via `cross` or the `aarch64` GitHub runner if available; otherwise skip that target), `x86_64-apple-darwin`, `aarch64-apple-darwin`, `x86_64-pc-windows-msvc`, and `aarch64-pc-windows-msvc`, package each as `herdr-mem-cpu-load-<version>-<target>.tar.gz` (or `.zip` on Windows) containing the binary, `README.md`, and `LICENSE`, and attach them to a GitHub release created with `softprops/action-gh-release` (pinned by SHA) using the tag's CHANGELOG section as the body.
  - Add a `scripts/release.sh` (Unix) that bumps the version in `Cargo.toml`, `Cargo.lock`, and `herdr-plugin.toml` together, updates `CHANGELOG.md`, and creates the annotated tag, so the three version numbers cannot drift. Document it in `CONTRIBUTING.md`.
  - **Done.** `.github/workflows/release.yml` fires on `v*` tags. The `build` matrix covers all six targets: the two Linux ones natively (`ubuntu-latest` and `ubuntu-24.04-arm`, so neither `cross` nor QEMU is involved), both macOS ones from `macos-latest`, and both Windows ones from `windows-latest` — the image ships the MSVC ARM64 build tools, so `aarch64-pc-windows-msvc` cross-compiles there.
  - `aarch64-unknown-linux-gnu` carries `optional: true` and the job is `continue-on-error: ${{ matrix.optional == true }}`, so a repository without the arm64 runner label loses that one archive instead of the whole release.
  - Each job first checks the tag against `[package] version` in `Cargo.toml` and refuses to publish an archive whose name would lie. Packaging is one `shell: bash` step on all three runners: a `dist/herdr-mem-cpu-load-<version>-<target>/` directory holding the binary, `README.md`, and `LICENSE`, then `tar -czf` or `7z a -tzip`. Simulated locally end to end.
  - The `release` job downloads every archive with `merge-multiple: true`, builds the body with `scripts/changelog_section.py`, and publishes through `softprops/action-gh-release@b4309332` (v3) with `fail_on_unmatched_files: true`. `contents: write` is scoped to that job only.
  - `scripts/release.sh` is POSIX `sh`: it validates the version, refuses a dirty worktree or an existing tag, rewrites the first line-initial `version = ` of `Cargo.toml` and `herdr-plugin.toml` with `awk` (dependency versions are inline, so they never match), refreshes `Cargo.lock` through `cargo metadata`, turns `## [Unreleased]` into `## [X.Y.Z] - DATE` with a fresh empty `Unreleased` above it, runs `scripts/check_manifest.py`, then commits and creates the annotated tag. It pushes nothing. Verified end to end in a scratch clone: all three version numbers and the lock moved to 0.2.0 together.
  - `scripts/changelog_section.py` prints one release's changelog body, falling back to a pointer at `CHANGELOG.md` when the version has no section, so a missing entry can never block a tag.
  - `CONTRIBUTING.md` has a "Releasing" section covering the dry run, the real run, the two pushes, and why bumping a version by hand is not supported.

- [x] Add repository hygiene files and metadata:
  - `CHANGELOG.md` in Keep a Changelog format with an `Unreleased` section summarizing everything built in Phases 01–04 (one-line port, daemon tokens, macOS/Windows backends, config, colors, watch popup).
  - `CONTRIBUTING.md`: build/test commands, the cross-target `cargo check` commands from Phase 03, how to test against a live herdr (`plugin link`, running the daemon by hand, `herdr plugin log list`), conventional commit subjects, and the release script.
  - `.editorconfig` (UTF-8, LF, 4-space Rust indentation, 2-space TOML/YAML/Markdown).
  - Update `Cargo.toml` `include` list so `cargo package` ships only `src/**`, `README.md`, `LICENSE`, `CHANGELOG.md`, and `herdr-plugin.toml`; run `cargo package --allow-dirty --list` to confirm.
  - README: add CI and license badges, and a "Publishing to the herdr marketplace" note explaining that the GitHub repository needs the `herdr-plugin` topic and the manifest at the repository root for automatic indexing (see `/home/matt/src/herdr/docs/next/website/src/content/docs/marketplace.mdx`).
  - **Done.** `CHANGELOG.md` is Keep a Changelog with one `## [Unreleased]` section covering Phases 01-04 — the one-line port and the original's flags, the three graph styles, tmux/powerline/ANSI colour, the daemon and its fourteen sidebar tokens with hysteresis, the singleton lock, the manifest, the macOS and Windows backends with the emulated load average and the BSD stub, the `config.toml` layer with `[thresholds]` and `mem_graph_lines`, and `--watch`. It also has a `### Fixed` entry for the original's Windows `ullAvailPhys` bug, which this port deliberately does not reproduce.
  - `CONTRIBUTING.md` covers requirements, the four checks CI runs in order, the five cross-target `cargo check` commands from Phase 03, testing against a live herdr (`plugin link`, running the daemon by hand with `--log-file --verbose`, `herdr plugin log list`, the popup pane and actions), conventional commit subjects with examples, and the release script.
  - `.editorconfig`: UTF-8, LF, final newline, trailing whitespace trimmed, 4-space Rust with a 100 column guide, 2-space TOML/YAML/Markdown/JSON. Markdown keeps its trailing whitespace, which is how it spells a hard line break.
  - `Cargo.toml` gained an `include` list. `cargo package --allow-dirty --list` now prints exactly `src/**` (including `src/config/default_config.toml`), `README.md`, `LICENSE`, `CHANGELOG.md`, `herdr-plugin.toml`, and the `Cargo.toml`/`Cargo.lock`/`.cargo_vcs_info.json` cargo always adds. No playbooks, no workflows, no scripts, no `tests/`.
  - README: CI and Apache-2.0 badges under the title, a "Publishing to the herdr marketplace" section (the `herdr-plugin` topic plus a parseable manifest on the default branch, the 30 minute reindex, why a malformed or drifted manifest drops the listing), and a short "Contributing" section pointing at `CONTRIBUTING.md` and `CHANGELOG.md`.


- [x] Hardening pass over the whole crate, reading every module in `src/` with fresh eyes:
  - Replace any remaining `unwrap`/`expect` outside tests with proper error propagation; the daemon must never panic.
  - Make sure every `unsafe` block has a `// SAFETY:` comment and is minimal; run `cargo clippy --all-targets -- -D warnings -W clippy::undocumented_unsafe_blocks`.
  - Confirm `build_tokens` output never exceeds 80 characters even with `graph_lines = 60` (clamp the bar width so the widest token fits; add a unit test with extreme widths) and that a `graph_lines` above 64 is rejected by CLI validation with a helpful message.
  - Check that `--interval` larger than 3600 is rejected and that `ttl_ms` is always clamped to herdr's accepted range 1–86400000.
  - Run the daemon under `valgrind --tool=none`-free alternatives: watch RSS with `ps -o rss` over 60 seconds at `--interval 1` and confirm it stays flat (no growth from the history ring or log handles).
  - Run `cargo doc --no-deps` and fix broken intra-doc links; add crate-level docs in `src/main.rs` describing the module map.
  - **Done.** No `unwrap`, `expect`, `panic!`, `unreachable!`, or `todo!` exists outside a `mod tests` anywhere in `src/`; the new code adds none. Every failure path in the daemon logs and continues or logs and stops.
  - All fourteen `unsafe` blocks (seven in `sys/macos.rs`, three in `sys/windows.rs`, two in `sys/unix_common.rs`, two in `daemon/lock.rs`) already carried a `// SAFETY:` comment. `cargo clippy --all-targets -- -D warnings -W clippy::undocumented_unsafe_blocks` is clean on the host and on `x86_64-apple-darwin` and `x86_64-pc-windows-msvc`, which is the only way the macOS and Windows blocks get linted at all.
  - Bar widths are now *narrowed* rather than values truncated. `TokenOptions::fitted` measures the three readings, works out the widest token (`sys_status` always binds — it carries all three segments and a bar), and caps both `graph_lines` and `mem_graph_lines` at what is left of the 80 characters. One budget is applied to both bars so the sidebar rows still line up. `GraphStyle::Vertical` is one character wide whatever is asked, so it is left alone.
  - `a_wide_bar_is_narrowed_rather_than_the_reading_truncated` covers 0, 10, 40, 60, and 64 cells across all three styles and both CPU modes, against both the ordinary sample and an `extreme_sample` (four terabytes of memory, load averages in the thousands, 256 threads). It asserts every token validates *and* that the reading survives intact at the end of the value, so a pass cannot come from truncation.
  - `-g`/`--mem-graph-lines` above 64 is rejected by `parse_graph_lines` with `65 cells is wider than the 64 cell maximum; a sidebar token holds 80 characters, and a wider bar leaves no room for the reading beside it`.
  - `--interval` is now `1..=3600` and `--ttl-ms` `1..=86400000`, both from the `MAX_INTERVAL_SECS`, `MIN_TTL_MS`, and `MAX_TTL_MS` constants so clap and `resolve` cannot drift. A `config.toml` is not range checked by clap, so `resolve` clamps `interval_secs`, `ttl_ms`, `graph_lines`, and `mem_graph_lines` as well — covered by `a_file_cannot_escape_the_ranges_the_command_line_enforces`. At the longest interval the derived ttl is 7,201,000 ms, comfortably inside herdr's range.
  - RSS over 60 seconds at `--interval 1` against a fake herdr answering two workspaces: 3016 KB rising to 3028 KB within the first 17 seconds and then flat for the remaining 43 samples. The 12 KB is the history ring filling to capacity and the log handle opening, not growth. Script and log are in `Working/`.
  - `cargo doc --no-deps` is warning-free. It had two `private_intra_doc_links` warnings for `COMMAND_TIMEOUT`, which is now `pub` and re-exported from `herdr::mod` — it is genuinely part of the client's contract, since the daemon's tick has to outlast it. `src/main.rs` gained crate-level docs: the four modes and a module map naming all nine library modules and what each owns.

- [x] Final verification and commit:
  - `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test --locked` (unit + integration) all pass on this machine, and the four cross-target `cargo check` commands from Phase 03 still pass.
  - `cargo build --release --locked`, then `herdr plugin link /home/matt/src/herdr-mem-cpu-load`, start the daemon by hand for 5 seconds, and confirm tokens appear in `herdr workspace list` one more time.
  - Commit with the message `ci: add cross-platform CI, release workflow, integration tests, and hardening`. Do not push or tag; leave that to the user after they add a GitHub remote.
  - **Done.** `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test --locked` all pass: 114 unit tests and 10 integration tests. The four Phase 03 cross-target `cargo check` commands still pass (`aarch64-apple-darwin`, `x86_64-apple-darwin`, `x86_64-pc-windows-msvc`, `x86_64-pc-windows-gnu`), as does `x86_64-unknown-freebsd`.
  - `cargo build --release --locked` then `herdr plugin link /home/matt/src/herdr-mem-cpu-load` against herdr 0.8.2, and the daemon run by hand for 5 seconds. `herdr workspace list` reported workspace `w9` carrying `cpu_status`, `mem_status`, `load_status`, `sys_status`, `cpu_history`, `cpu_ok`, `mem_ok`, and `load_ok` — the five status keys plus the one level token per metric that applied, with the other six cleared. The daemon log shows four ticks at one second apart.
  - Committed on `main` with the specified subject. Nothing pushed and nothing tagged; `Working/` and `.serena/` were left untracked, as in the earlier phases.
